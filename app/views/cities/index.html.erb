<div class="container">
    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
        <% @cities.each do |city| %>
            <%= link_to city.name, city_path(city), class: "dropdown-item" %>
        <% end %>
    </div>
</div>